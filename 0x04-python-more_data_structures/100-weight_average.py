#!/usr/bin/python3
def weight_average(my_list=[]):
    if my_list:
        numer = 0
        numer = map(lambda x, y: numer += x * y, )
        denom += y for x, y in my_list
        return numer / denom
    else:
        return 0
